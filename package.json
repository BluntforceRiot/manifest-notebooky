{
  "name": "manifest-notebooky",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4175",
    "typecheck": "tsc --noEmit",
    "playtest": "node scripts/playtest-browser.mjs",
    "package:review": "node scripts/package-review.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "jszip": "^3.10.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.16"
  }
}
